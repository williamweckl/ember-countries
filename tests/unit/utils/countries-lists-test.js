import countriesLists from '../../../utils/countries-lists';
import { module, test } from 'qunit';

module('Unit | Utility | countries lists');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = countriesLists();
  assert.ok(result);
});
